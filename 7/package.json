{
  "name": "my-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
	"dev": "VITE_NODE_VERSION=$(node -v) vite public --host",
	"build": "VITE_NODE_VERSION=$(node -v) vite build  public --outDir ../dist --emptyOutDir",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^5.0.0"
  }
}
